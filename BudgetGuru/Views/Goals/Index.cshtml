@model IEnumerable<BudgetGuru.Models.Goals>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GoalDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AchievementDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GoalDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AchievementDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GoalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GoalId })
        </td>
    </tr>
}

</table>
