@model IEnumerable<BudgetGuru.Models.Expenditures>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExpenditureDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpenditureCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExpenditureDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpenditureCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExpenditureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExpenditureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExpenditureId })
        </td>
    </tr>
}

</table>
